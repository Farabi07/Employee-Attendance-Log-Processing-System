import React from "react";
import PlaceholderScreen from "../../components/PlaceholderScreen";

// Phase 2 (UI) + Phase 3 (Stripe payout-card setup, batch payroll,
// pending cash-out approval, CSV/PDF/Excel export via expo-file-system).
export default function Payroll() {
  return <PlaceholderScreen title="Payroll & Wallets" />;
}
