import React from "react";
import PlaceholderScreen from "../../components/PlaceholderScreen";

// Phase 2: shift/branch/leave-type CRUD, live rotating QR display
// (the hardest single-component port — see the plan), geofence config.
export default function Roster() {
  return <PlaceholderScreen title="Roster" />;
}
