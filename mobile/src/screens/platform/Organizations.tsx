import React from "react";
import PlaceholderScreen from "../../components/PlaceholderScreen";

// Later phase: platform-owner super-admin dashboard (tenant list, global
// subscription pricing, per-org payout commission override).
export default function Organizations() {
  return <PlaceholderScreen title="Organizations" />;
}
